//a function that takes an array of integers, and returns a new array with every other element
/* START
- iterate through array using forEach method
- SET empty array newArray variable for new array 
- SET iterator = 0
- WHILE iterator <= length of array {
  
  iterator = iterator + 1
}
*/