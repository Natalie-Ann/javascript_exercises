//a function that takes an array of strings, and returns a string that is all those strings concatenated together
/* START
- SET iterator = 0 to start at first element of array
- SET newString undefined
- WHILE iterator is <= length of array, 
  SET newString = 
  iterator = iterator + 1
*/
